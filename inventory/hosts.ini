# ============================================================================
# b08x.workstation inventory
# ============================================================================

[workstations]
# The Pop!_OS 24.04 test VM (libvirt, bridged on bridge0).
# Requires `sudo apt install -y openssh-server` inside the guest.
popvm  ansible_host=192.168.41.42  ansible_user=b08x

# Uncomment once gir has actually been reinstalled with Pop!_OS.
# Until then it is still Fedora 43 and the distribution assert stops the run.
# gir  ansible_connection=local

[workstations:vars]
ansible_python_interpreter=/usr/bin/python3
