# Managed by Ansible
NODE_ENV=production
# Resolves over ai-net
DATABASE_URL=postgresql://postgres:{{ vault_postgres_password | default(change_me_in_vault) }}@ai-postgres:5432/postgres
CLICKHOUSE_URL=http://ai-clickhouse:8123
CLICKHOUSE_USER=clickhouse
CLICKHOUSE_PASSWORD={{ vault_clickhouse_password | default(change_me_in_vault) }}
NEXTAUTH_SECRET={{ vault_langfuse_nextauth_secret | default(generate_a_secret) }}
SALT={{ vault_langfuse_salt | default(generate_a_salt) }}
NEXTAUTH_URL=http://localhost:3000
