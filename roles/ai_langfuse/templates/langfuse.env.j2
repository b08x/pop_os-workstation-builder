# Managed by Ansible
NODE_ENV=production
# Resolves over ai-net
DATABASE_URL=postgresql://postgres:{{ ai_langfuse_db_password }}@ai-postgres:5432/postgres
CLICKHOUSE_URL=http://ai-clickhouse:8123
CLICKHOUSE_USER=clickhouse
CLICKHOUSE_PASSWORD={{ ai_langfuse_clickhouse_password }}
NEXTAUTH_SECRET={{ vault_langfuse_nextauth_secret | default(ai_langfuse_nextauth_secret) }}
SALT={{ vault_langfuse_salt | default(ai_langfuse_salt) }}
NEXTAUTH_URL=http://localhost:3000
